{
  "name": "api",
  "version": "1.0.0",
  "description": "API server for streams app using JSON server",
  "main": "index.js",
  "scripts": {
    "start": "json-server -p 3001 -w db.json"
  },
  "author": "mohit bajaj",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.1"
  }
}
